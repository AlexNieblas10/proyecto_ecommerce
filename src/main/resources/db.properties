# FashionHub - Database & App Configuration
# Priority: system environment variables > .env file > these defaults
# Copy .env.example to .env and set your values there (never commit .env).

db.host=localhost
db.port=3306
db.name=ecommerce_db
db.user=root
db.password=changeme

# JWT secret — use a long random string in production
jwt.secret=fashionhub-super-secret-key-change-me-in-production

# Mail (optional)
mail.smtp.host=smtp.gmail.com
mail.smtp.port=587
mail.from=noreply@fashionhub.com
mail.user=
mail.password=
